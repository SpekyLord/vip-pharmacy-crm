/**
 * Doctor Routes
 *
 * Endpoints:
 * GET /api/doctors - Get all doctors
 * GET /api/doctors/:id - Get doctor by ID
 * POST /api/doctors - Create new doctor
 * PUT /api/doctors/:id - Update doctor
 * DELETE /api/doctors/:id - Delete doctor
 * GET /api/doctors/region/:regionId - Get doctors by region
 * GET /api/doctors/:id/visits - Get doctor's visit history
 * POST /api/doctors/:id/assign - Assign doctor to rep
 */

const express = require('express');
const router = express.Router();

// TODO: Import doctor controller
// TODO: Import auth and role middleware
// TODO: Define routes with proper middleware

module.exports = router;
